/**
* SVG_to_Vertex 0.3.0
* @author Stan le Punk
* @see https://github.com/StanLepunK
*/


// show full shape
Vec2 pos is used to set the position of your PShape
float scale is used to scale your shape, the value 1. is a full size
Vec2 jitter is use to add a jitting effect on your point

void draw_vertex_SVG() ;
void draw_vertex_SVG(Vec2 pos) ;
void draw_vertex_SVG(Vec2 pos, float scale) ;
void draw_vertex_SVG(float scale) ;
void draw_vertex_SVG(Vec2 pos, float scale, Vec2 jitter) ;

// show a specific part of your shape

int num_group_vertex()
@ return the quantity of chidren of your shape

int ID is used to show a particular group, if this group don't exist Processing show nothing
void draw_specific_child_vertex_SVG(pos, scale, jitter, ID)







